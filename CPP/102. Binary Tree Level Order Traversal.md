# [102. Binary Tree Level Order Traversal](https://leetcode.com/problems/binary-tree-level-order-traversal/)

```C++
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
 
// 其实就是前序遍历，我的方法有点笨重了，参考人家的c++： https://discuss.leetcode.com/topic/4894/one-of-c-solutions-preorder
class Solution {
public:
    vector<vector<int>> levelOrder(TreeNode* root) {
        vector<vector<int>> res;
        if (root == NULL) return res;
        vector<int> r{root->val};
        res.push_back(r);
        dfs(root, res, 1);
        
        return res;
    }
    
public:
    void dfs(TreeNode* root, vector<vector<int>>& res, int level){
        if(root != NULL){
            if (root->left == NULL && root->right == NULL) return;
            if (res.size() < level+1) {
                vector<int> temp;
                res.push_back(temp);
            }
            if(root->left != NULL) res[level].push_back(root->left->val);
            if(root->right!= NULL) res[level].push_back(root->right->val);
            if(root->left != NULL) dfs(root->left, res, level+1);
            if(root->right!= NULL) dfs(root->right, res, level+1);
        }
    }
```
